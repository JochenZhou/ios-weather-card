name: Build & Release ZIP

# 🔑 权限：允许写入仓库内容和 Release
permissions:
  contents: write

# 🔹 支持两种触发方式
on:
  push:
    tags:
      - 'v*'           # Push 标签自动触发
  release:
    types: [published] # 手动 Draft 发布 → Publish Release

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
    # 1️⃣ 拉取仓库
    - name: Checkout repository
      uses: actions/checkout@v4

    # 2️⃣ 创建 zip 包（包含 JS + 图标 + hacs.json）
    - name: Create ZIP package
      run: |
        mkdir build
        cp ios-weather-card.js build/
        cp ios-weather-const.js build/
        cp ios-weather-icons.js build/
        cp ios-weather-style.js build/
        cp ios-weather-editor.js build/ || true
        cp -r weathericons build/
        cp hacs.json build/
        cd build
        zip -r ../ios-weather-card.zip .

    # 3️⃣ 上传 zip 到 Release
    - name: Upload ZIP to GitHub Release
      uses: softprops/action-gh-release@v1
      with:
        files: ios-weather-card.zip
        draft: false        # 🔹 立即发布，不当草稿
        prerelease: false   # 🔹 标记为正式版本
      env:
        GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
